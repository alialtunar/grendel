# Progress log

> Auto-updated by the `run-all` skill. This is your window into an unattended run:
> what finished, what is blocked, and where the run stopped.

<!-- Entries are appended below, one per phase. Examples:

phase 01: DONE — auth scaffolding + login endpoint, 14 tests green
phase 02: DONE — session store + refresh tokens, 9 tests green
phase 03: BLOCKED — tester STATUS: BLOCKING, 2 integration tests fail (DB timeout)

-->

phase 01: DONE — foundation: gauntlet CLI (run/list/report), Pydantic config + YAML loader, structured logging, TargetAdapter ABC + HTTP adapter (openai/anthropic/openrouter/ollama/openai-compatible + custom providers), RunRecord model. 41 tests green, ruff clean. reviewer + tester STATUS: PASS.
phase 02: DONE — attack-pack schema (Attack model + discriminated success_when union), packloader with strict validation + license gating (allowlist + opt-in), 12 bundled Apache-2.0 packs (6 prompt-injection + 6 jailbreak, OWASP 2025/ATLAS mapped), CLI list --packs wired. 79 tests green (41+38), ruff clean. reviewer + tester STATUS: PASS.
phase 03: DONE — async Runner engine: bounded concurrency (semaphore), rate-limit gate, seeded full-jitter retries + error taxonomy, per-request timeouts, token/cost accounting (pricing table), atomic resumable RunRecord persistence, CLI run wired (--pack/--dry-run/--resume/--out). Crash-safe (failing attack→ERROR, run completes). 107 tests green (79+28), deterministic & offline, ruff clean. reviewer + tester STATUS: PASS.
phase 04: DONE — Scoring T1 (deterministic canary/regex + refusal markers) + T2 (pluggable Classifier protocol + bundled pure LexicalClassifier, ambiguous-only escalation), verdict schema locked (PASS=defended, FAIL=attack-succeeded), ScoreDetail, ASR overall + by-category + metrics_summary, scorer wired into runner + report. Contract updates: asr denominator scored-only, executed sends now scored. 137 tests green (+30), deterministic & offline, ruff clean. reviewer + tester STATUS: PASS.
phase 05: DONE — ⭐ TUI scoreboard (Textual): pure zero-Textual ScoreboardState view-model (per-category grid, ASR, elapsed/cost/progress, severity flags, failure list, repro string), additive Runner on_attempt hook for live updates, ScoreboardApp (header/grid/detail panes) with hotkeys (j/k filter f/c/s explain copy-repro rerun quit), cli run --tui. Live bridge via post_message (no deadlock). 175 tests green (+38), Textual run_test pilot tests deterministic across 3 runs, offline, ruff clean. reviewer + tester STATUS: PASS.
phase 06: DONE — T3 LLM-judge (opt-in, default OFF): Judge protocol + FakeJudge + adapter-backed AdapterJudge (reuses target machinery), versioned rubric (RUBRIC_V1) + N-vote ensemble (majority, tie→defended), JudgeCheck handling, contested-only escalation via async score_async (sync score unchanged). Benign controls (is_control, control/ namespace) + utility-under-attack metric in report. No import cycle. 226 tests green (+51), inert when disabled, offline (FakeJudge/FakeAdapter), ruff clean. reviewer + tester STATUS: PASS.
phase 07: DONE — Agent mode (the differentiator): instrumented sandbox (ToolRegistry/ToolContext/ToolCall, no real side effects), python/agent/mcp target adapters (mcp lazy-optional), demo weak agent, SAFE side-effect assertion grammar+evaluator (no eval), T4 outcome scorer wired into score_async (judge-independent), tool-abuse pack (5 side-effect attacks). Key property: tool-abuse scores FAIL/T4 vs weak agent (tool actually called), SKIPPED vs plain LLM. 305 passed +1 skipped (mcp-absent guard), offline, ruff clean. reviewer + tester STATUS: PASS.
phase 08: DONE — MCP-aware attacks: MCP surface model (McpObservation: tool descriptions + approved/current snapshots on the adapter), SAFE protocol-assertion evaluator mcp_assert.py (description_changed/contains/any_contains/tool_shadowed, no eval), McpAssertCheck success_when + T4 MCP scorer in score_async (judge-independent), fake MCP server + weak MCP agent, mcp pack (4: poisoning/poisoning-side-effect/rug-pull/shadowing). Key property: mcp pack FAIL/T4 vs fake server, SKIPPED vs non-mcp. 348 passed +1 skipped, offline (no real mcp), import doesn't pull mcp, ruff clean. reviewer + tester STATUS: PASS.
phase 09: DONE — open catalog: multi-source load_catalog (bundled + user pack_dirs + feed cache + staged, source-tagged, cross-source dedup with user>feed>bundled override), CatalogConfig, remote feed (FeedManifest + gauntlet update: license-gate-before-download, 1MiB cap, sha256 verify, content-coherence, atomic cache, offline via MockTransport), path-traversal-safe (pattern-validated entries + write path from validated attack fields), OWASP/ATLAS report breakdowns (by_owasp/by_atlas), docs/authoring-packs.md. 385 passed +1 skipped, offline, security+license tests pass, ruff clean. reviewer + tester STATUS: PASS.
phase 10: DONE — polish & ship (release-ready cut): CI/headless gate (run --fail-under, exit 0 pass / 3 ASR>threshold / 2 usage, artifact still written), score-diff (gauntlet diff: per-axis ASR deltas + newly-failing/fixed + cost/latency, text/json/md), HTML + Markdown report renderers (stdlib templating, no jinja2, deterministic, HTML-escaped adversarial payloads, backtick-safe MD fences), README with prominent authorized-use/defensive-only framing, VHS demo tape (gif out-of-band). 419 passed +1 skipped, offline (no browser/VHS), no new deps, ruff clean. reviewer + tester STATUS: PASS.

phase 11: DONE — rename gauntlet→Grendel + first-class TUI. M1: pure rename (src/gauntlet→src/grendel, all imports, pyproject scripts/hatch, logging root, Typer name, GauntletConfig→GrendelConfig / GauntletError→GrendelError, canary tokens GAUNTLET-*→GRENDEL-* incl. base64 payload re-encode + split-token payload, README/docs/SETUP/demo/examples; historical journal untouched) with test_rename.py guard (import/entrypoint/canary-pair/no-residue). M2: TUI elevation on pure-state+thin-renderer arch — asr_level/severity_level/verdict_glyph pure helpers → colored header ASR + grid ASR/severity (green/amber/red), live attack stream pane (StreamEvent + ScoreboardState.stream, newest-first, STREAM_LIMIT=12), tool-call visualization (FailureItem.tool_calls, detail pane). 434 passed +1 skipped (+15), state.py textual-free, offline, ruff clean. reviewer + tester STATUS: PASS.

phase 12: DONE — everything configurable from the terminal. Ad-hoc target from flags on `run` (no config file needed): http (--provider/--model/--base-url/--api-key-env/--header), python (--python), agent (--agent), mcp (--mcp-command/--mcp-url/--mcp-fake-client/--mcp-tool); exactly-one-source validation (exit 2). Flags synthesize a TargetConfig injected via GrendelConfig.with_cli_target (revalidated model_dump/model_validate round-trip; ConfigError propagates unwrapped; duplicate-name guard), reusing resolve_target_info/build_target unchanged. --target now optional. Dirs from CLI: run --output-dir (resume still wins), run/list --pack-dir. ProxyConfig (host/port/routes) + `grendel proxy` command previews/validates merged config (serving deferred to phase 13). 465 passed +1 skipped (+31), offline, ruff clean, no new deps. reviewer + tester STATUS: PASS.

phase 13: DONE — LLM-proxy: zero-touch agent testing (the core "test a real agent without any development" capability). New src/grendel/proxy.py: pure helpers (select_route longest-prefix, inject_attack per surface, build_upstream_request forwarding the agent's own Authorization/x-api-key — Grendel adds no key, extract_outcome for openai/anthropic/ollama tool_calls+text, tri-state None/[]/list) + async ProxySession.handle (route → inject attack → forward upstream → parse → Scorer.score_async T4-on-tool-intent / T1-on-text → append AttemptRecord → return upstream reply UNCHANGED) + stdlib http.server on a SINGLE persistent asyncio loop (run_coroutine_threadsafe, no cross-loop httpx reuse), thread-safe attack cycler, do_POST hardened (bad Content-Length→400, non-POST→405, never crashes). CLI `grendel proxy --serve --pack --judge --out` (requires route+attacks, judge via _build_judge_scorer, writes record on shutdown). No key leaks into the record. No new dependency (stdlib + httpx). 489 passed +1 skipped (+24; incl. localhost two-request round-trip + concurrency), offline (MockTransport upstream, ephemeral-port bind), ruff clean. reviewer + tester STATUS: PASS.

phase 14: DONE — open-corpus import (catalog enrichment; final phase). New src/grendel/corpus.py: GarakSource reads a garak Apache-2.0 corpus from a LOCAL path (.txt blank-line paragraphs + .json string/{prompt} lists; category dan*/jailbreak inferred), pluggable SOURCES registry; to_attack converts each prompt to a schema-valid Attack (owasp LLM01, atlas AML.T0054, surface prompt, severity medium, classifier success_when since open jailbreaks have no planted canary), wrapping ValidationError as PackError; import_entries/import_corpus pipeline: dedup-FIRST by normalized-payload hash (strip/collapse-ws/casefold) against the out dir AND within the run → incremental (re-run writes 0), then license-gate (ALLOWED_LICENSES), then atomic path-safe write <out>/<category>/<id>.yaml (id/category from the pattern-validated Attack → no traversal); YAML via model_dump(by_alias=True, mode="json") so enums are scalars. --limit breaks after N writes; --dry-run reports without writing. CLI `grendel import --source garak --path --out [--limit --dry-run --allow-unlisted-licenses]` (exit 2 on unknown source / missing path / conversion PackError). Output loads through the real packloader and is consumable via --pack-dir (end-to-end tested). 508 passed +1 skipped (+19), offline (bundled tests/fixtures/garak, no network, no garak install), ruff clean, no new deps. reviewer + tester STATUS: PASS.
phase 15: DONE — control-center TUI (`grendel tui`): a Textual home menu that drives every operation, wrapping existing engines (no new engine). Screens: Doctor (pure doctor_report → framed status panels: version, env-key presence [never values], targets/providers, catalog counts, proxy, judge), Config (edit run/judge/proxy/add-route/add-target + save_config → YAML round-trips through load_config), Run (target from a configured name or ad-hoc provider+model via extracted pure build_cli_target_config, pack select → builds the same Runner engine closure as `run --tui` and pushes the live scoreboard), Packs (list_packs table + garak import_corpus), Proxy (ProxyConfig validate + ProxySession/make_server+serve_forever in a thread + stop_server). Scoreboard refactored into a shared _ScoreboardUI mixin: ScoreboardApp (composes on its own screen → run --tui + app.query_one unchanged) + new ScoreboardScreen for the control center; bindings on the concrete classes (Textual skips mixin BINDINGS). Additive: bare `grendel` still help; CLI + run --tui identical. 532 passed +1 skipped (+24), offline (run_test pilots, monkeypatched serve seam, no real socket), ruff clean, no new deps. reviewer + tester STATUS: PASS.

post-15: PIVOT — removed the full-screen Textual TUI (grendel tui + run --tui + tui/ package + textual dep) per user preference; grendel is now a pure inline CLI. Added a branded banner (bare `grendel` prints a GRENDEL logo + command table + authorized-use notice) and an interactive `grendel config` (questionary arrow-key menu with a letter-menu fallback for non-TTY/tests). Offline tests updated; suite green.

phase 18: DONE — interactive home menu + self-explanatory setup. Bare `grendel` on a TTY now opens an arrow-key HOME MENU (Targets · API keys · Catalog/import · Run · Doctor · Settings[run/judge/proxy] · Save/Quit) — the primary way to manage AND operate the tool without memorising subcommands; non-TTY (pipe/CI/tests) and `--no-menu`/GRENDEL_NO_MENU still print the banner byte-identically (menu-vs-banner gate `_wants_home_menu`, with GRENDEL_FORCE_MENU as a deterministic test/power-user seam since CliRunner can't present a TTY). `main()` refactored to share `_autodiscover_config`/`_load_startup_config` across the subcommand + menu paths; the letter home menu reuses the granular helpers (targets/catalog editors, extracted `_edit_run/judge/proxy_letter|q`, `build_doctor_report`, corpus import — auto-wiring the imported dir into catalog.pack_dirs), plus `_menu_run` (dry-run + real via the existing build_target/_load_attacks/_execute/_summary engine; synthesizes an ad-hoc provider+model target on a THROWAWAY cfg copy so it never leaks into the saved config or collides on a re-run — a reviewer-caught bug). Self-explanatory add-target: `describe_target(tc,cfg)` reuses `resolve_target_info` on a preview copy and branches on api_style for the path suffix (`/chat/completions` · `/messages` · `/api/chat`, no drift), showing `POST <url> · model <m> · key env: <NAME>` (env-var NAME only, never a value) or `in-process call → module:attr`; type + provider choices are annotated (provider shows api_style + base_url/`needs base_url`), and a confirm/cancel step precedes the add (a ConfigError returns to the prompt, never crashes). API-key management: a menu section shows ✓/✗ presence per provider + the exact `export` hint, and can save a key to a gitignored `grendel.local.yaml` (new src/grendel/secrets.py: load/merge/save; malformed → ConfigError) that is merged into the env at startup only for unset vars (a real env var wins) — never written to grendel.yaml, value masked in output. Hygiene: grendel.local.yaml gitignored; conftest sets GRENDEL_NO_LOCAL_SECRETS module-level; README documents the home menu + local secrets. 548 passed +1 skipped (+14), offline, ruff clean, no new runtime dep. plan-reviewer (REVISE→addressed) + reviewer (2 BLOCKING rounds: ad-hoc-target leak, then provider-annotation gap → fixed) + tester STATUS: PASS. Post-18 UX refinements (user feedback, separately reviewer+tester PASS): the GRENDEL block-art wordmark now heads the home menu; a `packs` entry browses the attack catalog (list_packs, category filter, 50-row cap signalled); `_clear_and_logo` clears the terminal (TTY only) between redraws so menu levels don't stack; catalog entry relabelled "pack dir(s) (load sources)". Add-target redesigned to an intent-based menu ("what are you testing?" → [1] hosted model → provider+model · [2] my own endpoint/agent with an OpenAI-compatible chat URL → base_url+model → provider=openai-compatible · [3] a running app that calls an LLM → guided to the proxy, no target); the raw python/agent type left the menu (still supported by the engine + `run --python/--agent`); hosted picker excludes openai-compatible (no dead-end); every field carries a concrete example. 553 passed +1 skipped.
phase 16: DONE — easy-to-use CLI + logo + editable targets + LangGraph end-to-end. Logo helpers (render_logo/config_header) on the banner + config menu; friendlier hints (run-no-target guidance, run/import `next:` lines). `grendel config` targets are now add/remove-editable (list/add/remove submenu in both the arrow-key and letter shells): _add_target reuses cfg.with_cli_target for add-time validation (duplicate name / unknown provider rejected immediately); _remove_target clears a dangling judge.target. LangGraph demo agent in examples/langgraph-agent/ (send_email tool, env-configurable base_url so it targets OpenAI directly or `grendel proxy`); example-only deps, ast-parsed by a structural test, not imported by the suite. LIVE end-to-end vs real OpenAI (key via env only, never on disk): direct run 6 attacks asr 83.33%; zero-touch proxy in front of the LangGraph agent injected tool-abuse → the real model emitted send_email to attacker addresses, recorded asr 100% (LLM06). Found+fixed a real proxy bug (invalid bare tool message → valid assistant+tool pair) and added incremental record persistence (crash-safe, survives kill). 488 passed +1 skipped (+12), ruff clean, no secret on disk. reviewer + tester STATUS: PASS.
phase 17: DONE — completeness & flawless-CLI-usability sweep. Landing/discovery: banner COMMANDS now includes config+doctor, kept drift-proof by a test asserting it equals the registered Typer command set (9); a "New here?" quickstart on the banner; every command grew a copy-paste `--help` epilog example. cwd `./grendel.yaml` auto-discovery in the callback (so list/run see the configured catalog with no -c), with a discoverable `--no-config` flag AND a `GRENDEL_NO_AUTOCONFIG` env hatch set module-level in conftest so the repo-root grendel.yaml + ~1500-file catalog/ never leak into the suite. `grendel doctor` (new src/grendel/doctor.py, pure build_doctor_report): version, catalog totals by category/source, provider API-key env presence (set/not-set only — never the value), targets, proxy, a context-aware `next:` hint, colored on a TTY. `list --packs --category/--source` filters + a "Packs: N total" count line; an unknown filter value nudges with the valid set (exit 2, no silent-empty dead end). Config editor: provider now picked from the valid set (numbered letter menu + questionary select), plus an editable catalog submenu (add/remove pack_dirs, saved) in both shells; pure _add_pack_dir/_remove_pack_dir. Reviewer caught + fixed a real crash class: console writes threw UnicodeEncodeError on non-UTF-8 terminals (the user's Windows cp1254, and iso-8859-* which also lack em-dash/·). Fixed at the OUTPUT BOUNDARY: install_console_fallback() registers a codec error handler + reconfigures stdout/stderr at CLI startup so ANY un-encodable glyph degrades to ASCII instead of crashing (covers banner, config labels, list/doctor em-dashes, a report piped to stdout — everything), plus a pretty ASCII-art fallback (stream_supports_unicode + unicode= flag) for the banner/doctor/config_header wordmark. Verified via subprocess under PYTHONIOENCODING=iso-8859-5/9 (CliRunner masks this class — it always captures UTF-8 — so the regression test spawns a real interpreter). Hygiene: catalog/ gitignored (regenerated by one import, never tracked), garak documented as the optional `[corpora]` extra, the auto-import test bounded with --limit (suite ~32s→~20s), README covers config/import/catalog/doctor + the T2/T3 scoring caveat. First-run walkthrough test (grendel→run→config→import→list→doctor→run→report) locks the smooth newcomer path. 524 passed +1 skipped (+36), offline, ruff clean, no new runtime dep. plan-reviewer (REVISE→addressed) + reviewer (2 BLOCKING rounds: cp1254 crash, then residual em-dash/· on other codepages → boundary fix) + tester STATUS: PASS.



phase 19: DONE — agent-agnostic targets + finalize. M1: a 4th `api_style: custom` so any bespoke-JSON agent API is connected via CONFIG, not code — new CustomRequestSpec (path/body/headers template with {prompt}/{system}/{model}/{api_key} placeholders) + CustomResponseSpec (dotted text_path) on CustomProviderConfig, gated by a `_check_custom_shape` validator (request+response required iff custom; rejected otherwise); ProviderPreset carries request/response as PLAIN DICTS so targets/providers.py keeps zero runtime config import (no cycle); http_adapter grew `_subst` (replaces only the 4 known {keys}, leaves other braces verbatim) + `_dotted_get` (dict keys + numeric list indices → AdapterError, not a crash) + custom branches in _build_request/_parse_response (custom is not auth-requiring; key only via an explicit header). M2: the interactive add-target menu (letter + questionary) restructured to 2 intents — [1] a model (hosted) · [2] an agent (its own API) → sub-choice OpenAI-compatible chat URL OR custom JSON API; proxy left the add-target menu (demoted to indirect injection, _echo_proxy_hint rewired into `grendel proxy`'s preview); new pure `_add_custom_agent` (provider+target in one step, provider-name collision check distinct from the PRESETS check) with `_remove_target` orphan-provider cleanup; describe_target custom branch shows the real POST path + text_path (not the openai fallback). M3 finalize: architecture-consistency + hardcode sweep (removed dead `_APISTYLE_PROVIDER`; the `style == …` branches are correct api_style dispatch, by design; PRESETS/pricing literals are data), README "model vs agent" section, and a LIVE end-to-end vs a real bespoke-JSON echo agent (`POST /chat {"message"}→{"reply"}`) — 6 jailbreak attacks, ASR 66.67%, 0 errors, report rendered. 574 passed +1 skipped (+18: custom adapter, custom-agent menu/helper, proxy-preview niche, describe_target), offline, ruff clean, no new deps, no import cycle. plan-reviewer (REVISE→addressed) + reviewer (1 BLOCKING round: _echo_proxy_hint dead code → rewired) + tester STATUS: PASS. Post-19 UX refinement (user feedback, separately reviewer+tester PASS): the home-menu Run flow no longer asks for a free-text "target name" — new `_prompt_run_target(cfg)` offers configured targets as a SELECTABLE list (arrow-key questionary.select on a TTY, numbered typer.prompt fallback for pipes/tests) with an "ad-hoc model (provider+model)" option (`_ADHOC` sentinel) and straight-to-ad-hoc when no targets exist; `_run_target_label` labels each via resolve_target_info (ConfigError-safe). Ad-hoc throwaway-cfg leak-safety unchanged; run-menu test scripts updated + new test_home_menu_run_picks_configured_target. 575 passed +1 skipped, ruff clean. A second post-19 UX pass (user feedback, reviewer+tester PASS): the Run ad-hoc PROVIDER pick is now arrow-key on a TTY (new `_prompt_provider` → questionary.select, numbered fallback for pipes/tests; model stays free-text, labelled "type it — no fixed list"); and every menu choice carries a short purpose — settings items (run=engine speed & limits · judge=grade borderline replies with an LLM (T3) · proxy=indirect-injection endpoint) in both shells, a one-line "judge = an LLM that grades borderline attack replies (T3)" before the enable prompt, and `_judge_label` enriched. 575 passed +1 skipped, ruff clean.

phase 20: DONE — guided run flow (pick, don't type), user feedback on the ad-hoc Run path. M1: model suggestions are DATA, not a closed list — new `models: list[str]` on ProviderPreset (openai/anthropic/openrouter/ollama seeded); `_preset_models` reads them (ConfigError→[]); `_prompt_model` is now TTY-aware — arrow-key questionary.select over the provider's suggestions + a "✎ type another model…" escape (pre-highlighting the preset default_model when present), falling back to a typed prompt off-TTY or when a provider has no suggestions (openai-compatible/custom) — any model is always reachable. M2: the Run ad-hoc provider pick no longer preselects openai on a TTY and offers "+ add a custom agent (its own JSON API)" inline (`_ADD_CUSTOM` sentinel; `_prompt_provider` → str|None; letter fallback gains `[0] add a custom agent` ONLY under a new `allow_add_custom` flag so the shared `_targets_letter` caller is untouched); selecting it runs new `_prompt_custom_agent(cfg)` (prompts a name TTY-aware, delegates to the existing custom-agent flow, returns `(cfg, name)`; success detected via `name in new_cfg.targets`) and PERSISTS the configured agent into the returned cfg (vs the throwaway run_cfg used for a hosted ad-hoc model — the leak-safety split is now documented in `_menu_run`'s docstring). Every run prompt explains itself: one hint line before packs ("which attack packs to fire (blank = all bundled)") and dry-run ("plan only, no network / no real API calls"). 580 passed +1 skipped (+5: `_preset_models`, provider-letter add-custom gating, packs/dry-run hints, add-custom-inline round-trip persists to disk, declined-add adds nothing), offline, ruff clean, no new deps. plan-reviewer (REVISE→9 pts addressed: wire `_menu_run`→`_prompt_model`, gate `_prompt_provider_letter` add-custom, name+success detection in the wrapper, docstring, TTY-only no-preselect) + reviewer + tester STATUS: PASS.

post-20 UX refinement (user feedback, reviewer+tester PASS): the home-menu Run flow was too many questions and its output got wiped by the menu redraw. `_menu_run` now fires with sensible defaults — NO packs prompt (always all packs) and NO dry-run prompt (always a real run); a single `fire N attack(s) vs <target> (<provider>/<model>)?` confirm replaces both. New `_missing_run_key(target, cfg)` returns the API-key env var a real run needs but that's unset (provider.requires_key + resolved api_key_env absent; None for keyless/non-http); when missing, a one-line warning prints and the fire confirm defaults to No (so a keyless run isn't silently fired into a wall of auth errors). New `_pause_menu()` (`click.pause`, no-op off-TTY) holds the run summary on screen until Enter so the home loop's `_clear_and_logo` doesn't wipe it. Menu-only change — the `grendel run` subcommand's `--dry-run`/`--pack` flags are untouched (dry-run still reachable there). 581 passed +1 skipped (+6 net: fire-confirm cancel, no-packs/dry-run-prompt, missing-key warn+default-No, add-custom-inline persist, declined-add; dry-run menu test repurposed), offline, ruff clean. reviewer (noted an out-of-scope pre-existing quirk: openai-compatible has requires_key=True but api_key_env=None so no warning fires — not introduced here) + tester STATUS: PASS.

post-20 UX refinement #2 (user feedback "ilerleyişi görecek miyim?", reviewer+tester PASS): the menu Run was silent until the final summary — for a large catalog (the user fired 1517 attacks) that reads as a hang. Wired the existing `Runner.on_attempt` hook through `_execute(..., on_attempt=None)` and added `_run_progress(total)`: None off-TTY (pipes/tests stay clean), else a per-attempt callback that rewrites a single live line `\r  attacking… n/total · h hit(s)` (hit = `attempt.verdict.name == "FAIL"`, i.e. the attack got through), ended by a `typer.echo("")` before the summary. No concurrency hazard (Runner is single-thread cooperative asyncio; the callback is synchronous, fired after persistence outside the write lock with exceptions swallowed). 583 passed +1 skipped (+2: off-TTY-None, on-TTY counts attempts+hits), offline, ruff clean. reviewer + tester STATUS: PASS.

phase 21: DONE — full model lists + a live attack dashboard (user feedback: "çok daha fazla provider modeli … tüm providerler için", "ilerleme … kırmızı yeşil vs görsel şölen"). M1 live model discovery: new src/grendel/targets/model_list.py `fetch_models(preset, api_key)` asks the provider's OWN api which models it serves — openai `/models` (Bearer), anthropic `/models` (x-api-key + anthropic-version), ollama `/api/tags`; custom/no-base_url → []. Auth headers built CONDITIONALLY (never passes None → no TypeError); ANY failure (no key, non-2xx, network, bad shape) degrades to [] and never raises; de-duped, API order preserved. `_prompt_model` TTY branch now fetches live models (key from env) → label "model (openai · 63 live)", falling back to broadened static preset `models` ("· suggestions") then to a typed prompt — non-TTY path unchanged so pipes/tests never touch the network. Static fallback lists widened per hosted preset. M2 rich live dashboard: new src/grendel/live.py `LiveAttackRun` (progress bar + running ASR% + colored counters + a newest-first stream of recent verdicts, red=hit/green=defended/yellow=error) driven by the Runner `on_attempt` hook; `make_live_run` returns None off-TTY or without rich (→ plain `_run_progress` fallback, byte-identical to before). Crash-safety in depth for the user's Windows cp1254 console: `Console(legacy_windows=False)` (ANSI via stdout, not the win32 API that crashes on cp1254) + ASCII-degraded glyphs when `stream_supports_unicode` is False + the existing `install_console_fallback` + a per-callback try/except + Runner's own callback guard. Live ASR excludes `is_control` (matches records._asr); total==0 guarded. `_menu_run` uses `with live: asyncio.run(_execute(..., on_attempt=live.on_attempt))` on a TTY. pyproject adds explicit `rich>=13` (already transitive via typer). 598 passed +1 skipped (+15: 9 model_list incl per-style + missing-key + error/shape degrade, 6 live_dashboard incl counter/ASR/controls-excluded/total-guard/ASCII-degrade/render), offline (MockTransport, non-TTY gating), ruff clean. plan-reviewer (REVISE→5 pts addressed) + reviewer (PASS, 2 cosmetic nits) + tester STATUS: PASS.

phase 22: DONE — live-dashboard polish (user feedback: "kaç tanesi LLM'e gitti?", "eskilerini de görmek isterim / scroll", "çok daha güzel görsel şölen + GRENDEL logo"). Presentation-only redesign of src/grendel/live.py `LiveAttackRun._render`: a bordered `rich.Panel` (title "🐺 live attack run") containing — the GRENDEL wordmark (banner.render_logo, no_wrap+crop, dropped when width<66 to avoid mid-glyph wrap), a progress row (`rich.progress_bar.ProgressBar` in unicode mode / a manual `#`/`-` Text bar in ascii mode) with magenta `ASR%` + a LABELLED `{n}/{total} sent` counter (answers "how many went to the LLM" — one request per attempt, errors counted separately), a colored stat row (red hits / green defended / yellow errors), a separator (`Rule` unicode / inner-width `Text("-"*w)` ascii), and a height-filling stream of recent verdicts. New pure `_layout(height, width) -> (show_logo, visible, inner_width)` keeps the WHOLE panel within the terminal (total = logo_part + _CHROME + stream, `visible` floored at 3) so a tall terminal shows far more rows and a short one shrinks/drops the logo instead of scroll-duplicating; `recent` deque bumped to maxlen=200 (full 1517-row history stays in the saved record → `grendel report`, since a repainting Live can't scroll). Counters/ASR/on_attempt unchanged (still exclude is_control). cp1254 crash-safety extended to ALL frame chars: box.ASCII + manual bar + ascii separator + ascii glyphs when `stream_supports_unicode` is False, plus the kept `legacy_windows=False` + `install_console_fallback`; a broadened test asserts no █/░/✗/✓/⚠/·/─/│/━ leak in ascii mode. 601 passed +1 skipped (+3 net: logo/panel/sent render, width=80, logo-drop, ascii-box, `_layout` height-fit), offline, ruff clean, no new deps. plan-reviewer (REVISE→7 pts addressed) + reviewer (PASS, 1 cosmetic nit: ascii logo is 5 lines vs the shared 7-line budget → one fewer row, conservative) + tester STATUS: PASS.

code-review (phases 20-23, 8-angle finders + verify → 6 fixes, reviewer+tester PASS): (1) BUG — the menu ad-hoc run swallowed a model-picker cancel: `_prompt_model` returns "" on Esc/no-entry and `build_cli_target_config` only rejects `model is None`, so "" built a target and fired a billable junk run; added `if not model: return cfg` (matches the provider-cancel guard). (2) BUG — `live._layout` dropped the logo only on narrow WIDTH, so a short terminal (e.g. 12 rows) rendered a panel taller than the viewport → rich.Live scroll/flicker; now drops the logo on short HEIGHT too (`height >= _CHROME + logo_h + 3`), uses the correct per-mode logo height (`_LOGO_LINES_U=7` / `_LOGO_LINES_A=6` — the ascii wordmark is 5 rows, fixing a latent off-by-one), floors `visible` at 1, and passes `self.unicode`. (3) PERF — `_list_run_records` fully deserialized every record (all attempts, ~1517 each × 20 files) via pydantic just to build one-line labels; now `json.loads` + raw scalars (ASR/atk-count from raw attempt dicts), never materialising attempt objects (the chosen record is still fully parsed once in `_menu_reports` for the report). (4) `_run_progress` (plain off-TTY counter) counted control-prompt FAILs as hits, disagreeing with the dashboard/record; now excludes `is_control`. (5) `fetch_models` dropped the timeout when given an injected client; now passes `timeout=` on `get()` too. (Not changed: add-target's new live /models fetch is intended — degrades to static offline, never crashes; `_resolve_model_choices` static-list micro-dup left as-is.) 605 passed +1 skipped (+2: empty-model-cancels, `_layout` short-height/ascii), offline, ruff clean. reviewer + tester STATUS: PASS.

phase 24: DONE — jaw-dropping two-column live dashboard (user goal: "attackları çok güzel göster, gören 'bu neymiş ya' desin"). Redesigned src/grendel/live.py `LiveAttackRun` from a vertical stack into a bordered rich control-room: GRENDEL wordmark header; LEFT column = big magenta `ASR NN%` headline + bar, `fired n/total`, colored `✗ hits / ✓ defended / ⚠ errors`, and a live PER-CATEGORY breakdown (top ≤6 by volume, each a colored mini ASR bar + %); RIGHT column = the newest attacks streaming in (glyph+id+category, red/green/yellow by verdict) with the newest HIT spotlighted (`⚡`/reverse-red); FOOTER = spinner + full-width progress bar + labelled `n/total sent` + elapsed·throughput·ETA momentum. New state: `_by_cat` (hits/scored per category, controls excluded — matches records._asr), `_start`/`_clock` (injectable; None headless → `—`), `_cats`. `_layout` now returns `(show_logo, visible, cats, inner)` and budgets the WHOLE panel height (`_CHROME=4` + `_STATS_HEAD=4`; logo drops on narrow/short, categories degrade to 0) so it fits EXACTLY (verified rendered_lines==height for a 140-case sweep 30-120w × 7-40h × unicode/ascii). Crash-safety: every row is a single `Text(no_wrap=True, overflow=("ellipsis" if unicode else "crop"))` — long ids/categories hard-crop to one physical line (a reviewer-caught overflow: wrapped rows pushed the panel past the viewport), and ascii mode never emits "…" (U+2026, un-encodable on cp437/cp850/cp1254 — a second reviewer catch); box.ASCII + manual `_bar` + ascii glyphs/spinner + `legacy_windows=False` + `install_console_fallback` all kept. Off-TTY/no-rich plain-counter fallback + the run engine unchanged. 609 passed +1 skipped (+8: per-category tracking, momentum-with-injected-clock, latest-hit spotlight, `_layout` 4-tuple + short-height cats==0 + ascii-logo-height, long-id/long-cat no-overflow + no-ascii-ellipsis across sizes, broadened ascii-degrade), offline, ruff clean, no new deps. plan-reviewer (REVISE→10 pts addressed) + reviewer (2 BLOCKING rounds: row-wrap overflow, then ascii ellipsis leak → single-Text rows) + tester STATUS: PASS.

post-24 UX (user feedback, reviewer+tester PASS): (a) the menu run loaded the ~1517-file catalog SILENTLY after the target pick (a 20-30s apparent freeze) — new `_menu_load_attacks` prints "loading attack catalog…" on a cache MISS and caches by `_catalog_sig` (pack_dirs+feed/staged+flags) in a session-global `_MENU_ATTACK_CACHE` so re-runs are instant (run subcommand unchanged; conftest autouse-clears the cache between tests). (b) the ugly one-line post-run summary → a polished rich REPORT CARD (new src/grendel/report_card.py `render_card`): ASR headline+bar with severity-colored border, hit/defended/error tally, tokens/cost/duration, per-category ASR bars, an OWASP line, top-5 hits, saved path + full-report tip; cp1254-safe (ASCII-degrades glyphs/bars/box + the "→"/"·" + crop-not-ellipsis); `_print_report_card` prints it on TTY+rich, else the old plain output (off-TTY byte-identical). 613 passed +1 skipped (+4 report_card). reviewer + tester STATUS: PASS.

phase 25: DONE — in-app results browser (user: "attaklar bittikten sonra hangilerinden kaldı / hangilerinden geçti görmek isterim… çok yorucu"). After a run AND from `[r] reports`, `_menu_results(record)` lets the user browse per-attempt outcomes without leaving the menu (no `grendel report` round-trip, no LLM, no target-model dependency — reads the RunRecord). `_attempts_by_outcome` buckets non-control attempts into got-through (FAIL) / defended (PASS) / errors (ERROR), each sorted `(attack_id or "", attempt_id)`, matching the card's tallies. Filter picker (questionary on TTY / `h`/`d`/`e`/`b` letter fallback — any unknown/blank → back, no stdin-drain) → `_pick_attempt` (arrow-key / numbered, cap 30 + "+N more") → `_show_attempt` → new `report_card.render_attempt`: a bordered card with a colored verdict badge (`✗ GOT THROUGH` / `✓ defended` / `⚠ error` / `· skipped`, all ASCII-degraded via `_BADGE`), category, the PROMPT, the agent's RESPONSE (response_text|error), and WHY (score_detail tier+reason); prompt/response capped at 2000 chars (`_clip`). cp1254-safe (glyphs/box/arrow degrade, crop-not-ellipsis); `_show_attempt` prints the rich card on TTY+rich else a plain dump. Wired after the report card in `_menu_run`/`_menu_reports` (return value ignored; both still `return cfg`); the 4 run-/report-firing test scripts gained a blank line (= browser-back) before their trailing quit/save. 618 passed +1 skipped (+5: 3 render_attempt [detail/ascii/truncate], `_attempts_by_outcome` bucketing, letter-path drill-into-a-hit), offline, ruff clean, no new deps. plan-reviewer (REVISE→8 pts addressed incl. exact updated scripts, unknown→back, non-control errors) + reviewer + tester STATUS: PASS.

phase 26: DONE — zero-knowledge onboarding (user: "paketi indiren nasıl kullanıldığını bilmeyebilir, arayüz o kadar iyi olmalı ki direkt anlasın"). New `banner.render_welcome(*, color, unicode)`: a first-run orientation block — "New here? Grendel red-teams your AI…", "Get started:" + 3 numbered steps referencing the menu ITEM names (targets / run / doctor — not bracket keys, so it reads correctly in BOTH shells) + a doctor tip; ASCII-degrades (em-dash→"-", no ·/🐺 in ascii). Shown in `_home_letter` AND `_home_questionary` right after `_clear_and_logo`, before config_header, gated on `if not cfg.targets` (a fresh user) → vanishes once a target is configured (no nag). Banner untouched (already has "New here? · grendel doctor" + quickstart, guarded by test_first_run_walkthrough). 621 passed +1 skipped (+3: render_welcome unit incl. ascii-safety, welcome-shown-when-no-targets, welcome-hidden-with-a-target), offline, ruff clean, no new deps. plan-reviewer (REVISE→7 pts addressed: consistent shell order, item-names-not-bracket-keys, sys.stdout.isatty, banner-untouched) + reviewer + tester STATUS: PASS.

release prep (packaging for distribution, "insanlar nasıl indirecek"): pyproject PyPI-ready — dist name `grendel-redteam` (the `grendel` name is TAKEN on PyPI; command + import stay `grendel`), + authors/keywords/classifiers/urls (USERNAME placeholder for the repo). CLEAN-VENV INSTALL TEST caught a real packaging bug: `cli.py` imports `click` directly but modern typer (0.26) no longer hard-depends on it → added `click>=8` (and confirmed `rich` explicit); verified `pipx`-style install of the built wheel gives a working `grendel` CLI (banner + dry-run over the bundled packs). Added the Apache-2.0 LICENSE file (fetched canonical text), `.gitignore` +dist/+build/, a real README "Install" section (pipx / pip / from-source), and `.github/workflows/release.yml` (tag v* → test → build → PyPI Trusted Publishing). test_rename updated for the new dist name (command still asserted `grendel`). 621 passed +1 skipped, ruff clean. NOTE: not yet pushed to GitHub / published to PyPI (needs the user's repo + credentials); no git remote yet.

ALL PHASES COMPLETE
